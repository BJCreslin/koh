<changeSet id="%s" author="%s">
    <preConditions onFail="MARK_RAN" onError="MARK_RAN">
        <sqlCheck expectedResult="1">
            SELECT COUNT(*) FROM profile WHERE profile_name = '%s'
        </sqlCheck>
    </preConditions>

    <comment>
        DOSIE-%s Добавление пермишинов для вкладки %s профиля %s
    </comment>
        %s
    <rollback>
        %s
    </rollback>

</changeSet>